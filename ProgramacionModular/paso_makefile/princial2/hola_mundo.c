#include <funcion.h> 

int main() 
{ 
    escribeHolaMundo ();
    return 0;
}

/*
CFLAGS=-I../funcion2
export CLFAGS 
make HolaMundo, fallara porque tiene dos archivos .c
 */
// compilar con el Makefile
